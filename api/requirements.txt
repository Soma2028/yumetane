# API実行に必要な最小限の依存関係（Renderにデプロイするのはこちら）。
# notebooks/での分析・前処理に使うライブラリ（jupyter, scikit-learn等）は
# requirements-dev.txt を参照。api/app/ の実行時コードはpandas/numpyの
# 行列計算のみで、scikit-learnには依存していない
# （PCA/Ridge回帰はnotebooksでの前処理時にのみ使用し、結果は
# data/processed/riasec_transform.json に定数として保存済み）。
fastapi==0.141.1
uvicorn[standard]==0.52.4
pandas==3.0.5
numpy==2.5.3
